To run this project:

1. on the host
- `mkdir build` & `cd build`
- `cmake ..` to configure
- `cd interception`
- `make cuda_server` to compile the cuda server
- `./cuda_server` to run the executable

2. on the guest
- 